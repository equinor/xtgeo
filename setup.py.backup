#!/usr/bin/env python
#===============================================================================
# A "makefile" setup file for xtgeo library
# By Jan C. Rivenaes
# 
# Commands: (use python (for 2.7) or python3 as 'python' below
#    -- make tests:
#    python setup.py tests
#    -- install as private user:
#    python setup.py install --user
#    
#===============================================================================


import os
import sys
from distutils.sysconfig import get_python_lib

#from distutils.core import ... this is outdated?
#JRIV has reinstalled setuptools to my user... via pip install
from setuptools import find_packages, setup, find_packages

import glob

os.umask(5)


setup(name='xtgeo',
      description='XTGeo library for Python',
      author='JRIV',
      version='1.0',
      #package_dir={'xtgeo': 'xtgeo'},
      packages=find_packages(exclude=['tests']),
      test_suite="tests",
      )

os.umask(7)
